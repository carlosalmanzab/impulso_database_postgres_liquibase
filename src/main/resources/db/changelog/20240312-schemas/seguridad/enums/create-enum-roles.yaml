databaseChangeLog:
  - changeSet:
      id: create-enum-seguridad_roles
      author: carlos.almanza
      changes:
       - sql:
           sql: |
             CREATE TYPE seguridad.roles AS ENUM (
             'ADMIN',
             'USER'); 